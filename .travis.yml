language: scala

branches:
  only:
    - master

notifications:
  email:
    recipients:
      - github@quadstingray.com

jdk:
  - oraclejdk8
  - oraclejdk9
  - openjdk7

sudo: false
addons:
  apt:
    packages:
      - oracle-java8-installer